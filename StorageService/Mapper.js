var fs = require('fs')
var pickle_service = require('./pickle_service')

var pickle_or_not = async () => {
    if(fs.existsSync('allocation_data.json')){
        let obj = null
        try{

        } catch(err) {
            console.log(err)
        }
    }
}

var map = async (hashval) => {

}